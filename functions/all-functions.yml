provider:
  name: faas
  gateway: http://localhost:8080

functions:
  create-account:
    lang: node
    handler: ./create-account
    image: create-account
    environment_file:
      - env.remote.yml
